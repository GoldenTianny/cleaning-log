// 이 파일을 config.js 로 복사한 뒤 본인 Supabase 값으로 채우세요.
// config.js 는 .gitignore 에 의해 GitHub에 올라가지 않습니다 (anon key는 공개돼도 RLS로 안전하지만, 습관상 분리).
window.CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  SUPABASE_ANON_KEY: 'YOUR-ANON-KEY'
};
